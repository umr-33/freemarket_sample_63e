<li class="card-item">
  <%= link_to "/items/#{item.id}", class: "card-item", method: :get do %>
    <%= image_tag item.images[0].image, class: 'hoge', size: '188x188' if item.images[0]%>
      <h3 class="A"><%="¥#{item.price}"%></h3>
    <figcaption class="card-text">
      <%= item.description %>
    </figcaption>
  <% end %>
</li>
